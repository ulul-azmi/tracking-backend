const chai = require('chai')
const chaiHttp = require('chai-http')

const app = require('./src/app')

chai.use(chaiHttp)

describe('Income Report', () => {
  describe('Logged in user can create new income report')

})
